<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monitor.backend.mapper.AlarmTemplateMapper">

    <resultMap id="BaseResultMap" type="com.monitor.backend.entity.AlarmTemplate">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="subject" property="subject"/>
        <result column="content" property="content"/>
        <result column="content_type" property="contentType"/>
        <result column="is_default" property="isDefault"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, subject, content, content_type, is_default, create_time, update_time
    </sql>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM alarm_template
        ORDER BY id DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM alarm_template
        WHERE id = #{id}
    </select>

    <select id="findDefault" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM alarm_template
        WHERE is_default = 1
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO alarm_template(name, subject, content, content_type, is_default, create_time, update_time)
        VALUES(#{name}, #{subject}, #{content}, #{contentType}, #{isDefault}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        UPDATE alarm_template
        SET name = #{name}, subject = #{subject},
            content = #{content}, content_type = #{contentType}, is_default = #{isDefault}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM alarm_template WHERE id = #{id}
    </delete>

</mapper>
