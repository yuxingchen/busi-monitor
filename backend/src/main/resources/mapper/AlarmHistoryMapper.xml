<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monitor.backend.mapper.AlarmHistoryMapper">

    <resultMap id="BaseResultMap" type="com.monitor.backend.entity.AlarmHistory">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="channel_id" property="channelId"/>
        <result column="trigger_time" property="triggerTime"/>
        <result column="trigger_type" property="triggerType"/>
        <result column="trigger_value" property="triggerValue"/>
        <result column="threshold_value" property="thresholdValue"/>
        <result column="message" property="message"/>
        <result column="is_success" property="isSuccess"/>
        <result column="error_message" property="errorMessage"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, task_id, channel_id, trigger_time, trigger_type, trigger_value,
        threshold_value, message, is_success, error_message
    </sql>

    <select id="findByTaskId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM alarm_history
        WHERE task_id = #{taskId}
        ORDER BY trigger_time DESC
        LIMIT #{limit}
    </select>

    <select id="findRecent" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM alarm_history
        ORDER BY trigger_time DESC
        LIMIT #{limit}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM alarm_history
        ORDER BY trigger_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO alarm_history(task_id, channel_id, trigger_time, trigger_type, trigger_value,
                                  threshold_value, message, is_success, error_message)
        VALUES(#{taskId}, #{channelId}, #{triggerTime}, #{triggerType}, #{triggerValue},
               #{thresholdValue}, #{message}, #{isSuccess}, #{errorMessage})
    </insert>

</mapper>
