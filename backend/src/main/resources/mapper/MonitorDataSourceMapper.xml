<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monitor.backend.mapper.MonitorDataSourceMapper">

    <resultMap id="BaseResultMap" type="com.monitor.backend.entity.MonitorDataSource">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="url" property="url"/>
        <result column="username" property="username"/>
        <result column="password_encrypted" property="passwordEncrypted"/>
        <result column="driver_class_name" property="driverClassName"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, url, username, password_encrypted, driver_class_name, create_time, update_time
    </sql>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM monitor_datasource
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM monitor_datasource
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO monitor_datasource(name, url, username, password_encrypted, driver_class_name, create_time, update_time)
        VALUES(#{name}, #{url}, #{username}, #{passwordEncrypted}, #{driverClassName}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE monitor_datasource
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="url != null">url = #{url},</if>
            <if test="username != null">username = #{username},</if>
            <if test="passwordEncrypted != null">password_encrypted = #{passwordEncrypted},</if>
            <if test="driverClassName != null">driver_class_name = #{driverClassName},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM monitor_datasource WHERE id = #{id}
    </delete>

</mapper>

