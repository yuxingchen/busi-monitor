# **业务监控系统（Busi-Monitor）项目架构与功能点总结**

## **封面页**
```
项目名称：业务监控系统（Busi-Monitor）
项目代号：busi-monitor
版本号：v0.0.1-SNAPSHOT
文档版本：v1.0
编写部门：技术部
编写人：自动生成
审核人：待定
发布日期：2025-12-20
保密级别：内部
```

### **修订记录**
| 版本 | 修订日期 | 修订人 | 修订说明 |
|------|----------|--------|----------|
| v1.0 | 2025-12-20 | 系统自动生成 | 初始版本 |

---

## **一、项目概览**

### **1.1 项目背景**
- **业务驱动力**：为企业提供统一的业务监控与告警平台，解决多数据源监控分散、告警不及时、服务器资产管理困难等问题
- **行业背景**：企业IT运维监控需求日益增长，需要集成化、可视化的监控解决方案
- **战略意义**：提升运维效率，保障业务系统稳定运行，降低故障响应时间

### **1.2 项目目标**
#### 业务目标：
- 短期目标（3个月）：
  - 实现多数据源SQL监控任务配置与执行
  - 实现服务器资产管理与基础监控
  - 实现可视化仪表盘与告警功能
- 中期目标（1年）：
  - 支持复杂的SQL工作流编排
  - 实现跨库数据关联分析
  - 完善监控模板库

#### 技术目标：
- 架构目标：前后端分离、模块化设计、易扩展
- 性能目标：支持周期性定时任务执行
- 质量目标：代码结构清晰，易于维护

### **1.3 项目范围**
#### 包含范围：
- 功能范围：数据源管理、监控任务管理、服务器资产管理、仪表盘可视化、告警配置、工作流编排
- 用户范围：运维人员、技术管理人员
- 数据范围：业务数据库监控数据、服务器性能指标

#### 排除范围：
- 不包含：用户认证与权限管理（可后期扩展）
- 不包含：分布式集群部署支持

---

## **二、总体架构**

### **2.1 架构设计原则**
1. **前后端分离**：Vue3前端 + Spring Boot后端，接口标准化
2. **模块化设计**：按功能划分控制器、服务、实体层
3. **可扩展性**：支持动态数据源、多种监控模板
4. **易用性**：可视化配置，拖拽式仪表盘设计

### **2.2 架构图**

#### 2.2.1 逻辑架构图
```
┌─────────────────────────────────────────────────────────────┐
│                        表现层 (Frontend)                      │
│  Vue 3 + Element Plus + ECharts + Vue Flow                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐     │
│  │ Dashboard │ │ 任务配置  │ │ 服务器管理 │ │ 工作流设计器  │     │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘     │
├─────────────────────────────────────────────────────────────┤
│                       应用层 (Backend)                        │
│  Spring Boot 3.2 + MyBatis + Quartz                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │Controller│ │ Service  │ │  Mapper  │ │  Entity  │         │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘         │
├─────────────────────────────────────────────────────────────┤
│                      数据层 (Database)                        │
│  MySQL 8.0 (主数据库) + 动态外部数据源                          │
└─────────────────────────────────────────────────────────────┘
```

#### 2.2.2 系统交互图
```
用户 → 前端(Vue3) → REST API → 后端(Spring Boot)
                                      │
                    ┌─────────────────┼─────────────────┐
                    ↓                 ↓                 ↓
              MySQL主库          外部数据源          远程服务器
              (配置存储)        (业务数据采集)       (SSH脚本执行)
```

### **2.3 技术选型**

#### 后端技术栈：
| 技术领域 | 选型 | 版本 | 选型理由 |
|----------|------|------|----------|
| 开发框架 | Spring Boot | 3.2.1 | 主流Java Web框架，生态成熟 |
| 持久层框架 | MyBatis | 3.0.3 | 灵活的SQL映射，支持动态SQL |
| 任务调度 | Spring Quartz | 内置 | 支持Cron表达式定时任务 |
| 数据库 | MySQL | 8.0 | 成熟稳定的关系型数据库 |
| SSH连接 | JSch | 0.1.55 | Java SSH客户端，支持密码/密钥认证 |
| JSON处理 | Jackson | 内置 | Spring Boot默认JSON库 |
| 简化开发 | Lombok | 最新 | 减少样板代码 |

#### 前端技术栈：
| 技术领域 | 选型 | 版本 | 选型理由 |
|----------|------|------|----------|
| 前端框架 | Vue | 3.5.x | 渐进式框架，响应式设计 |
| 构建工具 | Vite | 7.2.x | 极速开发体验 |
| UI框架 | Element Plus | 2.12.x | 丰富的企业级组件 |
| 状态管理 | Pinia | 3.0.x | Vue 3官方推荐状态管理 |
| 路由 | Vue Router | 4.6.x | 官方路由方案 |
| HTTP客户端 | Axios | 1.13.x | 主流HTTP库 |
| 图表库 | ECharts | 6.0.x | 强大的可视化图表 |
| 流程图 | Vue Flow | 1.48.x | 节点拖拽式流程设计 |
| 时间处理 | Day.js | 1.11.x | 轻量级日期处理库 |
| 布局组件 | Grid Layout Plus | 1.1.x | 拖拽式网格布局 |

---

## **三、核心功能模块**

### **3.1 功能模块划分**
```
┌─────────────────────────────────────────────────────────────┐
│                      业务监控系统功能模块                       │
├─────────────────────────────────────────────────────────────┤
│ 1. 数据源管理模块                                              │
│    - 外部数据源CRUD（MySQL连接配置）                           │
│    - 密码加密存储                                              │
│    - 连接测试功能                                              │
├─────────────────────────────────────────────────────────────┤
│ 2. 监控任务模块                                                │
│    - SQL监控任务配置                                           │
│    - Cron定时调度                                              │
│    - 结果类型支持（标量值/数据集）                               │
│    - 阈值告警规则配置                                          │
│    - 历史记录存储                                              │
├─────────────────────────────────────────────────────────────┤
│ 3. 服务器资产管理模块                                          │
│    - 服务器信息管理（IP/端口/认证方式）                         │
│    - 分组管理（树形结构）                                       │
│    - SSH连接测试                                              │
│    - IP段批量导入                                              │
├─────────────────────────────────────────────────────────────┤
│ 4. 监控模板模块                                                │
│    - 预置模板（CPU/内存/磁盘/端口检测等）                       │
│    - 自定义脚本模板                                            │
│    - 模板分类（基础/组件/应用）                                 │
├─────────────────────────────────────────────────────────────┤
│ 5. 服务器监控任务模块                                          │
│    - 服务器与模板绑定                                          │
│    - SSH脚本远程执行                                           │
│    - 采集结果记录                                              │
│    - 批量任务分配                                              │
├─────────────────────────────────────────────────────────────┤
│ 6. 仪表盘模块                                                  │
│    - 多仪表盘管理                                              │
│    - 可拖拽Widget布局                                          │
│    - 多种图表类型（折线/柱状/饼图/表格）                        │
│    - 实时数据刷新                                              │
├─────────────────────────────────────────────────────────────┤
│ 7. 工作流编排模块                                              │
│    - 可视化流程设计器                                          │
│    - 多步骤SQL编排                                             │
│    - 跨数据源查询                                              │
│    - 内存JOIN支持                                              │
│    - 循环/常量步骤支持                                         │
│    - 结果表自动创建与索引                                       │
├─────────────────────────────────────────────────────────────┤
│ 8. 告警配置模块                                                │
│    - 告警通道管理（邮件/短信/钉钉）                             │
│    - 告警模板管理                                              │
│    - 告警历史记录                                              │
└─────────────────────────────────────────────────────────────┘
```

### **3.2 功能详细说明**

#### 模块1：数据源管理服务
**功能点：**
1. **数据源CRUD**
   - 功能描述：管理外部MySQL数据源连接配置
   - 数据模型：`monitor_datasource` 表
   - 字段包含：名称、JDBC URL、用户名、密码（加密）、驱动类名

2. **动态数据源**
   - 功能描述：运行时动态创建数据库连接
   - 技术实现：`DataSourceManager` + `DynamicSqlExecutor`
   - 支持多数据源并行查询

#### 模块2：监控任务服务
**功能点：**
1. **任务配置**
   - 功能描述：配置SQL监控脚本、执行周期、告警规则
   - 数据模型：`monitor_task` 表
   - 结果类型：SCALAR（单值）、DATASET（数据集）

2. **定时调度**
   - 技术实现：Spring Quartz + Cron表达式
   - 服务类：`TaskMonitoringService`
   - 支持动态刷新任务调度

3. **告警检测**
   - 规则配置：JSON格式（operator + value）
   - 支持的运算符：`>`, `<`, `=`, `>=`, `<=`, `!=`

#### 模块3：服务器资产管理
**功能点：**
1. **资产管理**
   - 数据模型：`server_asset` 表
   - 认证方式：PASSWORD（密码）、KEY（密钥）
   - 安全存储：AES加密敏感信息

2. **分组管理**
   - 数据模型：`server_group` 表
   - 支持树形层级结构

3. **IP段导入**
   - 服务类：`IpRangeParser`
   - 支持CIDR格式（如192.168.1.0/24）
   - 支持范围格式（如192.168.1.1-100）

#### 模块4：工作流编排服务
**功能点：**
1. **工作流定义**
   - 数据模型：`monitor_workflow` + `monitor_workflow_step`
   - 步骤类型：SQL、CONSTANT、LOOP
   - 可视化设计：Vue Flow组件

2. **工作流执行**
   - 服务类：`WorkflowExecutorService`
   - 上下文变量传递
   - 跨数据源内存JOIN：`MemoryJoinExecutor`
   - SQL占位符解析：`WorkflowSqlParser`

3. **结果存储**
   - 动态表创建：`DynamicTableService`
   - 自动索引：`IndexFieldCollector`
   - 执行历史：`monitor_workflow_execution`

#### 模块5：仪表盘服务
**功能点：**
1. **仪表盘管理**
   - 数据模型：`dashboard` + `dashboard_widget`
   - 网格布局配置
   - 默认仪表盘设置

2. **Widget组件**
   - 类型：TASK（任务组件）、WORKFLOW（工作流组件）
   - 布局属性：位置(x,y)、尺寸(w,h)、层级(z-index)
   - 图表配置：类型、X/Y轴映射

---

## **四、数据库设计**

### **4.1 数据库表结构**

| 序号 | 表名 | 说明 | 主要字段 |
|------|------|------|----------|
| 1 | `monitor_datasource` | 数据源配置 | name, url, username, password |
| 2 | `monitor_task` | 监控任务 | name, datasource_id, sql_script, cron_expression |
| 3 | `monitor_record` | 监控记录 | task_id, execution_time, result_number, result_json |
| 4 | `alarm_channel` | 告警通道 | name, type, config |
| 5 | `alarm_template` | 告警模板 | name, channel_type, subject, content |
| 6 | `alarm_history` | 告警历史 | task_id, channel_id, trigger_value, message |
| 7 | `server_group` | 服务器分组 | name, description, parent_id |
| 8 | `server_asset` | 服务器资产 | name, ip, port, username, auth_type |
| 9 | `monitor_template` | 监控模板 | name, category, collect_type, collect_script |
| 10 | `server_monitor_task` | 服务器监控任务 | server_id, template_id, collect_script |
| 11 | `dashboard` | 仪表盘 | name, grid_cols, grid_rows, is_default |
| 12 | `dashboard_widget` | 仪表盘组件 | dashboard_id, widget_type, task_id, chart_config |
| 13 | `monitor_workflow` | 工作流 | name, cron_expression, timeout_seconds, index_fields |
| 14 | `monitor_workflow_step` | 工作流步骤 | workflow_id, step_order, step_type, sql_script |
| 15 | `monitor_workflow_execution` | 工作流执行记录 | workflow_id, status, start_time, end_time |

### **4.2 ER关系**
```
monitor_datasource ←─── monitor_task ←─── monitor_record
                                    │
                                    └─── dashboard_widget
                                    
server_group ←─── server_asset ←─── server_monitor_task ←─── monitor_template

dashboard ←─── dashboard_widget

monitor_workflow ←─── monitor_workflow_step
       │
       └─── monitor_workflow_execution
```

---

## **五、代码结构**

### **5.1 后端目录结构**
```
backend/
├── pom.xml                           # Maven配置
├── src/main/java/com/monitor/backend/
│   ├── BusiMonitorApplication.java   # 启动类
│   ├── config/                       # 配置类
│   ├── controller/                   # 控制器层 (10个)
│   │   ├── AlarmController.java
│   │   ├── DashboardController.java
│   │   ├── DataSourceController.java
│   │   ├── MonitorTemplateController.java
│   │   ├── ServerAssetController.java
│   │   ├── ServerGroupController.java
│   │   ├── ServerMonitorTaskController.java
│   │   ├── StatsController.java
│   │   ├── TaskController.java
│   │   └── WorkflowController.java
│   ├── entity/                       # 实体类 (15个)
│   ├── mapper/                       # MyBatis Mapper接口 (15个)
│   ├── dto/                          # 数据传输对象
│   └── service/                      # 服务层 (12个)
│       ├── DataSourceManager.java
│       ├── DynamicSqlExecutor.java
│       ├── DynamicTableService.java
│       ├── EncryptionService.java
│       ├── IndexFieldCollector.java
│       ├── IpRangeParser.java
│       ├── MemoryJoinExecutor.java
│       ├── SqlPlaceholderService.java
│       ├── SshExecutorService.java
│       ├── TaskMonitoringService.java
│       ├── WorkflowExecutorService.java
│       └── WorkflowSqlParser.java
└── src/main/resources/
    ├── application.yml               # 应用配置
    ├── logback-spring.xml            # 日志配置
    ├── mapper/                       # MyBatis XML映射 (15个)
    └── sql/
        └── schema.sql                # 数据库建表脚本
```

### **5.2 前端目录结构**
```
frontend/
├── package.json                      # NPM配置
├── vite.config.js                    # Vite配置
├── index.html                        # 入口HTML
└── src/
    ├── main.js                       # Vue入口
    ├── App.vue                       # 根组件（含侧边栏导航）
    ├── style.css                     # 全局样式
    ├── router/
    │   └── index.js                  # 路由配置
    ├── api/                          # API调用
    ├── assets/                       # 静态资源
    ├── components/                   # 公共组件
    └── views/                        # 视图组件 (8个)
        ├── Dashboard.vue             # 监控看板
        ├── DashboardEditor.vue       # 仪表盘编辑器
        ├── WorkflowDesigner.vue      # 工作流设计器
        ├── TaskConfig.vue            # 任务配置
        ├── ServerAsset.vue           # 服务器资产
        ├── MonitorTemplate.vue       # 监控模板
        ├── AlarmConfig.vue           # 告警配置
        └── DataSource.vue            # 数据源管理
```

---

## **六、API接口清单**

### **6.1 RESTful API**

| 模块 | 接口路径 | 方法 | 说明 |
|------|----------|------|------|
| **数据源** | /api/datasource | GET | 获取数据源列表 |
|  | /api/datasource | POST | 创建数据源 |
|  | /api/datasource/{id} | PUT | 更新数据源 |
|  | /api/datasource/{id} | DELETE | 删除数据源 |
| **监控任务** | /api/task | GET | 获取任务列表 |
|  | /api/task | POST | 创建任务 |
|  | /api/task/{id} | PUT | 更新任务 |
|  | /api/task/{id}/run | POST | 手动执行任务 |
| **仪表盘** | /api/dashboard | GET | 获取仪表盘列表 |
|  | /api/dashboard/{id} | GET | 获取仪表盘详情 |
|  | /api/dashboard/default | GET | 获取默认仪表盘 |
|  | /api/dashboard/{id}/layout | POST | 保存布局 |
| **工作流** | /api/workflows | GET | 获取工作流列表 |
|  | /api/workflows/{id} | GET | 获取工作流详情 |
|  | /api/workflows | POST | 创建工作流 |
|  | /api/workflows/{id} | PUT | 更新工作流 |
|  | /api/workflows/{id}/execute | POST | 执行工作流 |
|  | /api/workflows/{id}/result | GET | 获取执行结果 |
|  | /api/workflows/test-step | POST | 测试步骤SQL |
| **服务器** | /api/server/asset | GET | 获取服务器列表 |
|  | /api/server/asset | POST | 添加服务器 |
|  | /api/server/asset/{id}/test | POST | 测试SSH连接 |
|  | /api/server/group | GET | 获取分组列表 |
| **监控模板** | /api/template | GET | 获取模板列表 |
|  | /api/template | POST | 创建模板 |
| **告警** | /api/alarm/channel | GET | 获取告警通道 |
|  | /api/alarm/template | GET | 获取告警模板 |
|  | /api/alarm/history | GET | 获取告警历史 |

---

## **七、部署与运行**

### **7.1 环境要求**
| 组件 | 版本要求 |
|------|----------|
| JDK | 17+ |
| Node.js | 18+ |
| MySQL | 8.0+ |
| Maven | 3.6+ |

### **7.2 后端启动**
```bash
cd backend
mvn clean package
java -jar target/backend-0.0.1-SNAPSHOT.jar
# 服务运行在 http://localhost:8080
```

### **7.3 前端启动**
```bash
cd frontend
npm install
npm run dev
# 服务运行在 http://localhost:5173
```

### **7.4 配置说明**
```yaml
# application.yml
server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/busi_monitor
    username: root
    password: root
```

---

## **八、总结**

### **8.1 项目特点**
1. **统一监控平台**：集成SQL数据监控和服务器监控于一体
2. **可视化配置**：拖拽式仪表盘、流程化工作流设计
3. **灵活扩展**：支持动态数据源、自定义监控模板
4. **跨库分析**：工作流支持跨数据源内存JOIN

### **8.2 技术亮点**
- Spring Boot 3.2 + MyBatis 现代化后端架构
- Vue 3 + Element Plus + ECharts 响应式前端
- Vue Flow 实现可视化工作流设计
- JSch 实现SSH远程脚本执行
- 动态SQL占位符解析与变量替换

### **8.3 后续规划**
- [ ] 用户认证与权限管理
- [ ] 告警通知实际推送实现
- [ ] 监控数据趋势分析
- [ ] 集群部署支持

---

**文档状态**：[✓] 草稿 [ ] 评审中 [ ] 已批准 [ ] 已归档

**相关文档**：
- [SpringBatch在亿级数据跨库关联查询的实现方案.md](./SpringBatch在亿级数据跨库关联查询的实现方案.md)
